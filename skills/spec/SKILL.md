---
name: spec
description: >
  Interview the user to clarify requirements, write a YAML feature spec, and produce
  failing tests for each contract. Asks clarifying questions in phases (happy path,
  edge cases, error cases, constraints), writes the spec to
  $AGENTS_DIR/projects/<project>/specs/<feature>.yaml, and writes one failing test per
  contract before any implementation exists.
---

Follow the specification workflow in `.agents/workflows/specification.md`.
