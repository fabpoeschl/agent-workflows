---
name: fix
description: >
  Diagnose and fix a bug, then commit the result. Starts from a failing test or
  reproduction case, identifies the root cause (not the symptom), implements the
  minimal fix, re-runs the tests, and commits with a fix: message following the
  project's commit conventions. Does not push.
---

Follow the bugfix workflow in `.agents/workflows/bugfix.md`.
